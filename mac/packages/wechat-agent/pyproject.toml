[project]
name = "wechat-agent"
version = "0.1.0"
description = "WeChat Automation Agent"
dependencies = [
    "ax-core",
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
ax-core = { workspace = true }

[project.scripts]
send-wechat = "wechat_agent.cli:main"
